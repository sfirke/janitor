use_first_valid_of <- function(...){
  # check var lengths
  # check var types?  Initially should be same class, maybe eventually coerce to character 
  vars <- as.list(substitute(list(...)))
  # initialize results vector of appropriate length and class
  # fill it using 2 for loops
  
}